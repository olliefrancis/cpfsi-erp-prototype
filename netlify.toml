[build]
  publish = "."

[functions]
  directory = "netlify/functions"

# Set SLACK_FEEDBACK_WEBHOOK in Netlify UI (Site configuration → Environment variables).
# Do not commit the webhook URL to the repository.
