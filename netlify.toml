[build]
  publish = "."

[functions]
  directory = "netlify/functions"

# Set FEEDBACK_WEBHOOK_URL in Netlify (Site configuration → Environment variables).
# Point v1/v2 at this site via feedback-config.js on GitHub Pages.
